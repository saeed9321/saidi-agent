import{_ as t,o as a,c as s,a3 as d}from"./chunks/framework.B02wg2PO.js";const k=JSON.parse('{"title":"The Marketplace","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/marketplace.md","filePath":"concepts/marketplace.md"}'),i={name:"concepts/marketplace.md"};function o(n,e,r,l,c,h){return a(),s("div",null,[...e[0]||(e[0]=[d(`<h1 id="the-marketplace" tabindex="-1">The Marketplace <a class="header-anchor" href="#the-marketplace" aria-label="Permalink to &quot;The Marketplace&quot;">​</a></h1><p>The Marketplace is how capability moves between Saidi instances. A <strong>package</strong> is a JSON document describing an agent, a department, a skill, or an MCP server; a <strong>registry</strong> is a list of those packages; a <strong>source</strong> is an adapter that turns some upstream catalog into that same shape.</p><p>Nothing in a package executes at install time. Packages are JSON, every file inside is a path plus a string, and remote registries only ever index package URLs — so a package can be read in full before anything touches disk.</p><p>For the step-by-step install path, see <a href="/docs/guides/marketplace">Installing from the Marketplace</a>. This page is what sits underneath it.</p><h2 id="the-four-package-kinds" tabindex="-1">The four package kinds <a class="header-anchor" href="#the-four-package-kinds" aria-label="Permalink to &quot;The four package kinds&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Kind</th><th>Contains</th><th>Where it lands</th></tr></thead><tbody><tr><td><code>agent</code></td><td>Exactly one agent folder — <code>AGENT.md</code> plus whatever else it ships</td><td>A new directory in the agents root</td></tr><tr><td><code>department</code></td><td>1–20 agent folders plus a <code>department</code> block</td><td>One directory per member agent</td></tr><tr><td><code>skill</code></td><td>One skill folder, rooted at the folder and containing <code>SKILL.md</code></td><td><code>skills/&lt;slug&gt;/</code> on a chosen agent</td></tr><tr><td><code>mcp</code></td><td>One MCP server name plus a runnable stdio/http/sse config</td><td>The chosen agent&#39;s MCP config</td></tr></tbody></table><p><code>agent</code> and <code>department</code> install standalone. <code>skill</code> and <code>mcp</code> are <em>attachments</em> — they need a target agent, either an existing one or one created as part of the install. See <a href="/docs/concepts/agents-and-departments">Agents &amp; Departments</a> for why a capability belongs to one specialist rather than to everything.</p><h2 id="the-package-manifest" tabindex="-1">The package manifest <a class="header-anchor" href="#the-package-manifest" aria-label="Permalink to &quot;The package manifest&quot;">​</a></h2><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acme/researcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Researcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Gathers primary sources and writes cited briefs.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;author&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Acme&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;license&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;MIT&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tags&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;research&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;analysis&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;agents&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;researcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;files&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;AGENT.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;content&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }] }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Required</th><th>Notes</th></tr></thead><tbody><tr><td><code>schemaVersion</code></td><td>yes</td><td>Must be exactly <code>1</code></td></tr><tr><td><code>id</code></td><td>yes</td><td><code>^[a-z0-9][a-z0-9._/-]{1,100}$</code>. A <code>/</code> makes the leading segment a publisher namespace</td></tr><tr><td><code>kind</code></td><td>yes</td><td><code>agent</code> | <code>department</code> | <code>skill</code> | <code>mcp</code></td></tr><tr><td><code>name</code> <code>version</code> <code>description</code></td><td>yes</td><td>Trimmed and length-capped</td></tr><tr><td><code>author</code> <code>license</code></td><td>no</td><td>Free text</td></tr><tr><td><code>tags</code></td><td>no</td><td>Up to 12, 40 chars each</td></tr><tr><td><code>department</code></td><td>department only</td><td><code>{ name, lead? }</code>; <code>lead</code> must be one of the packaged agents</td></tr><tr><td><code>agents</code></td><td>agent/department</td><td><code>[{ name, files: [{ path, content }] }]</code></td></tr><tr><td><code>skill</code></td><td>skill only</td><td><code>{ slug, files }</code></td></tr><tr><td><code>mcp</code></td><td>mcp only</td><td><code>{ serverName, transport, config }</code></td></tr></tbody></table><p>A <strong>registry</strong> is thinner — it indexes packages without carrying them:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Saidi Marketplace&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;packages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acme/researcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Researcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;packageUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://…/package?id=acme%2Fresearcher&amp;version=1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A registry entry may also carry <code>transport</code>, <code>official</code>, <code>featured</code>, <code>verified</code>, <code>publisher</code>, <code>installCount</code>, <code>ratingAvg</code>, and <code>ratingCount</code>. Entries missing a valid <code>id</code>, <code>kind</code>, <code>name</code>, <code>version</code>, or <code>packageUrl</code> are dropped rather than rejected — a partly malformed registry still lists whatever parsed.</p><h2 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h2><p><code>validatePackage()</code> is the single gate. It runs client-side before preview, again in the orchestrator before install, and again server-side on every publish — the hosted service never trusts what a client posts.</p><table tabindex="0"><thead><tr><th>Guard</th><th>Rule</th></tr></thead><tbody><tr><td>Path traversal</td><td>No absolute paths, no empty segments, no <code>..</code></td></tr><tr><td>Forbidden dirs</td><td><code>.git</code>, <code>node_modules</code>, <code>.env</code> anywhere in a path</td></tr><tr><td>Secret files</td><td><code>.env*</code>, <code>secrets.json</code>, <code>credentials.json</code>, <code>id_rsa</code>, <code>*.pem</code> are refused</td></tr><tr><td>Per-file size</td><td>256 KB</td></tr><tr><td>Per-agent budget</td><td>80 files / 1 MB, scaled by member count for a department</td></tr><tr><td>Department ceiling</td><td>8 MB total, whatever the roster size</td></tr><tr><td>Agent slug</td><td><code>^[a-z0-9][a-z0-9-]{1,40}$</code>, unique within the package</td></tr><tr><td>Skill slug</td><td><code>^[a-z0-9][a-z0-9-]{1,60}$</code></td></tr><tr><td>Required file</td><td>Every agent needs <code>AGENT.md</code>; every skill needs <code>SKILL.md</code> at its root</td></tr><tr><td>MCP config</td><td>Must resolve to a runnable <code>command</code> (stdio) or a <code>url</code> (http/sse)</td></tr><tr><td>MCP secrets</td><td>A live-looking token in <code>env</code> or <code>headers</code> is refused outright — installers supply their own credentials</td></tr></tbody></table><p>Passing validation is not the same as being safe, so <code>packageWarnings()</code> produces a separate advisory list shown before install: executable-style content (<code>scripts/…</code>, <code>.sh</code>, <code>.py</code>, binaries), an <code>AGENT.md</code> that requests <code>permission: all</code>, bundled MCP configuration, and for MCP packages the exact command or URL the server will use.</p><h2 id="source-adapters" tabindex="-1">Source adapters <a class="header-anchor" href="#source-adapters" aria-label="Permalink to &quot;Source adapters&quot;">​</a></h2><p>The hosted Saidi catalog is the built-in source. Everything else is an adapter that normalises an upstream format into <code>RegistryItem</code> / <code>MarketplacePackage</code>, so the UI never learns a foreign shape and install stays one action.</p><table tabindex="0"><thead><tr><th>Source id</th><th>Label</th><th>Kinds</th><th>Pulls from</th><th>URL scheme</th></tr></thead><tbody><tr><td><code>hosted</code></td><td>Saidi</td><td>all four</td><td>The configured registry URL</td><td>plain <code>http(s)</code></td></tr><tr><td><code>skillsmp</code></td><td>SkillsMP</td><td><code>skill</code></td><td><code>skillsmp.com/api/v1/skills/search</code> — metadata plus a GitHub folder URL</td><td><code>skillsmp:</code></td></tr><tr><td><code>mcp-registry</code></td><td>MCP Registry</td><td><code>mcp</code></td><td><code>registry.modelcontextprotocol.io/v0/servers</code></td><td><code>mcp:</code></td></tr><tr><td><code>glama</code></td><td>Glama MCP</td><td><code>mcp</code></td><td><code>glama.ai/api/mcp/v1/servers</code></td><td><code>glama:</code></td></tr><tr><td><code>github</code></td><td>GitHub URL</td><td><code>skill</code>, <code>mcp</code></td><td>Any repo, folder, or file URL you paste</td><td><code>github:</code></td></tr></tbody></table><p>An adapter implements three methods: <code>search()</code> for the grid, <code>owns(packageUrl)</code> to claim a scheme, and <code>resolve(packageUrl)</code> to produce a full installable package.</p><p>Three of the five delegate resolution to the same GitHub extractor. SkillsMP and Glama index <em>repositories</em>, not run configurations, so installing one means walking the repo: a folder holding <code>SKILL.md</code> becomes a skill package; a folder holding <code>server.json</code>, <code>mcp.json</code>, or <code>.mcp.json</code> becomes an MCP package, with an npm package mapped to <code>npx -y &lt;name&gt;</code>, pypi to <code>uvx</code>, and oci to <code>docker</code>. Environment variables the registry declares become empty placeholders you fill in yourself.</p><p>SkillsMP and Glama are search-first — an empty query returns nothing rather than a default page. GitHub URL import has no search at all; it only resolves what you paste.</p><p>All outbound fetches go through an SSRF-guarded helper: every hop is DNS-resolved and re-checked against a private/reserved/link-local/metadata deny list, redirects are followed manually, and credentialed or non-<code>http(s)</code> URLs are refused. Responses are capped at 1.25 MB with a 20-second timeout.</p><h2 id="caching-ingest-and-capture" tabindex="-1">Caching, ingest, and capture <a class="header-anchor" href="#caching-ingest-and-capture" aria-label="Permalink to &quot;Caching, ingest, and capture&quot;">​</a></h2><p>Browsing an external source would otherwise hammer it on every keystroke, so three layers of state sit under the marketplace data root:</p><table tabindex="0"><thead><tr><th>Path</th><th>Holds</th></tr></thead><tbody><tr><td><code>marketplace-cache/</code></td><td>Raw search results and resolved packages. Searches use a 10-minute TTL; a failed fetch falls back to a stale entry, which is also what makes browsing work offline</td></tr><tr><td><code>marketplace-catalog.json</code></td><td>The ingested Saidi catalog — items a superadmin surfaced by searching, deduplicated by id</td></tr><tr><td><code>marketplace-pruned.json</code></td><td>Ids proven not installable, so a re-search never resurrects a dead listing</td></tr><tr><td><code>marketplace-packages/</code></td><td>Fully captured packages, served as <code>saidi:&lt;id&gt;</code></td></tr></tbody></table><p>When a superadmin searches with two or more characters, the query fans out across every searchable adapter in parallel, results are merged and deduped by id, and the new rows are ingested into the shared catalog. In the background, <code>materializeCatalog()</code> resolves each new item (up to six at a time), pulls every file, revalidates the result under the <em>catalog</em> id, and freezes it into <code>marketplace-packages/</code>. The catalog entry is then repointed at <code>saidi:&lt;id&gt;</code>.</p><p>That repointing is the important part. Once captured, a package is self-contained: preview and install serve it from local disk with no external call, so it survives an upstream outage and installs identically for everyone. A resolve failure is triaged — a transient error (rate limit, timeout, network) leaves the live pointer to retry next search, while a definitive one prunes the listing so nobody is shown something they cannot install.</p><h2 id="installing-locally" tabindex="-1">Installing locally <a class="header-anchor" href="#installing-locally" aria-label="Permalink to &quot;Installing locally&quot;">​</a></h2><p><code>POST /api/marketplace/install</code> takes a <code>packageUrl</code> and does the following:</p><ol><li>Resolve the payload. A <code>saidi:</code> URL reads the captured package from disk. A scheme-prefixed URL goes through its adapter. Anything else is a plain remote fetch — and that last case is superadmin-only, because it is the SSRF surface.</li><li><code>validatePackage()</code> the result.</li><li>Check the caller may install that kind.</li><li>Install by kind.</li></ol><p>For <code>agent</code> and <code>department</code>, files are written into a staging directory first, each destination path re-checked to make sure it still resolves inside its own agent folder, and only then renamed into place. Overwriting an existing agent goes through a renamed backup, so a failure mid-way never leaves a half-written agent. Without <code>overwrite: true</code>, an existing directory of the same name causes the package to be reported as skipped.</p><p>For <code>skill</code> and <code>mcp</code>, the payload attaches to a target agent — named in <code>targetAgent</code>, or created inline via <code>createAgent</code>. If an agent was created for the install and the install then fails, the agent is rolled back. A stdio MCP command is validated before it is written, and cannot be installed at all on the hosted platform, where only <code>http</code>/<code>sse</code> servers are allowed.</p><p>Every install appends an <code>InstalledPackage</code> record:</p><table tabindex="0"><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><code>id</code> <code>version</code> <code>kind</code></td><td>What was installed</td></tr><tr><td><code>packageUrl</code></td><td>Where it came from — reused by update</td></tr><tr><td><code>digest</code></td><td>SHA-256 of the package JSON</td></tr><tr><td><code>installedAt</code></td><td>ISO timestamp</td></tr><tr><td><code>agents</code></td><td>The agent directories created, or the single attach target</td></tr><tr><td><code>targetAgent</code></td><td>skill/mcp only</td></tr><tr><td><code>attached</code></td><td><code>{ skillSlug }</code> or <code>{ mcpServer }</code> — the precise uninstall handle</td></tr></tbody></table><p>The manifest lives at <code>memory/marketplace-installed.json</code> locally, and per-org under the org&#39;s memory directory in cloud mode. Update re-installs the recorded id at <code>version=latest</code> with <code>overwrite: true</code>, reusing the recorded target. Uninstall reverses exactly what the record says was added, then drops the record.</p><h2 id="the-hosted-service" tabindex="-1">The hosted service <a class="header-anchor" href="#the-hosted-service" aria-label="Permalink to &quot;The hosted service&quot;">​</a></h2><p>The catalog runs as a standalone service, deliberately separate from the per-user orchestrator. It shares only <code>catalog.ts</code> — the types and validators — so it never pulls in the agent runtime.</p><table tabindex="0"><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/health</code></td><td>Liveness plus whether sign-in is configured</td></tr><tr><td><code>GET</code></td><td><code>/registry</code></td><td>The index: newest version of each active package. <code>kind</code>, <code>q</code>, <code>sort</code>, <code>limit</code>, <code>offset</code>, <code>featured</code></td></tr><tr><td><code>GET</code></td><td><code>/package</code></td><td>Full blob for <code>?id=&amp;version=</code> (<code>latest</code> allowed)</td></tr><tr><td><code>GET</code></td><td><code>/versions</code></td><td>Every published version of an id, newest first</td></tr><tr><td><code>POST</code></td><td><code>/publish</code></td><td>Re-validates, attributes, and namespace-scopes. Sign-in required</td></tr><tr><td><code>POST</code></td><td><code>/publish/delete</code></td><td>A publisher removes their own package or one version</td></tr><tr><td><code>POST</code></td><td><code>/install</code></td><td>Fire-and-forget install-count bump</td></tr><tr><td><code>GET</code>/<code>POST</code></td><td><code>/reviews</code></td><td>One review per reporter per package, ratings 1–5</td></tr><tr><td><code>POST</code></td><td><code>/report</code></td><td>Abuse report</td></tr><tr><td><code>POST</code></td><td><code>/auth/device/start</code> · <code>/auth/device/poll</code></td><td>GitHub device flow</td></tr><tr><td><code>GET</code></td><td><code>/account</code></td><td>The signed-in identity for a bearer token</td></tr><tr><td><code>POST</code></td><td><code>/admin/takedown</code></td><td>Requires the <code>x-admin-token</code> header</td></tr></tbody></table><p><code>id</code> is passed as a query or body parameter rather than a path segment throughout, because package ids may contain <code>/</code>.</p><p>Three rules do the load-bearing work. <strong>Versions are immutable</strong> — republishing the same <code>id@version</code> with a different digest is rejected, and an identical republish is idempotent, which blocks a silent supply-chain swap. <strong>Namespaces are owned</strong> — the first signed-in publisher of <code>login/*</code> owns it, <code>saidi</code> is reserved to configured maintainers, and you may only publish under your own GitHub login. <strong>Reports accumulate</strong> — five reports auto-hide a non-official package pending review, and reporter IPs are stored only as a salted hash.</p><p>Rate limits: 600 requests/minute globally, 30 publishes/hour, 60 reviews/hour, 60 reports/hour. Publish bodies are accepted up to 10 MB, above the 8 MB department ceiling to leave room for JSON overhead.</p><h2 id="pointing-at-a-different-registry" tabindex="-1">Pointing at a different registry <a class="header-anchor" href="#pointing-at-a-different-registry" aria-label="Permalink to &quot;Pointing at a different registry&quot;">​</a></h2><p>The catalog Saidi ships with is a hosted service, and that is what a new workspace uses. You can point at a different one from <strong>Settings → Connections</strong>.</p><p>A registry URL ending in <code>/registry</code> is treated as a publish-capable API base. Anything else is treated as a static catalogue: it browses and installs normally but cannot be published to. A bare GitHub repo URL is rewritten to the raw <code>marketplace.json</code> on its default branch.</p><p>Client-side state — <code>registryUrl</code>, the marketplace session token, the GitHub access token, and the SkillsMP API key — is held per workspace and encrypted at rest, because it includes bearer tokens. See <a href="/docs/reference/settings">Settings</a>.</p><p>See also: <a href="/docs/guides/marketplace">Installing from the Marketplace</a> · <a href="/docs/reference/http-api">HTTP API</a></p>`,48)])])}const g=t(i,[["render",o]]);export{k as __pageData,g as default};
