import{_ as t,o as d,c as s,a3 as a}from"./chunks/framework.B02wg2PO.js";const k=JSON.parse('{"title":"Events & the WebSocket Stream","description":"","frontmatter":{},"headers":[],"relativePath":"reference/events-and-websocket.md","filePath":"reference/events-and-websocket.md"}'),o={name:"reference/events-and-websocket.md"};function i(n,e,r,c,h,l){return d(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="events-the-websocket-stream" tabindex="-1">Events &amp; the WebSocket Stream <a class="header-anchor" href="#events-the-websocket-stream" aria-label="Permalink to &quot;Events &amp; the WebSocket Stream&quot;">​</a></h1><p>Two different things in Saidi are called &quot;events&quot;, and they do not touch each other:</p><ul><li><strong>WebSocket frames</strong> — what the orchestrator pushes to open dashboards so a run or chat renders live.</li><li><strong>Bus events</strong> — the normalized envelope every producer funnels into <code>emitEvent()</code>, which reflexes match on.</li></ul><p>This page documents both, plus the durable <code>RunEvent</code> that sits between them.</p><h2 id="the-endpoint" tabindex="-1">The endpoint <a class="header-anchor" href="#the-endpoint" aria-label="Permalink to &quot;The endpoint&quot;">​</a></h2><p>The WebSocket server is attached to the same HTTP server as the API, at path <code>/ws</code> — so the client connects to <code>wss://&lt;your-workspace-host&gt;/ws</code>.</p><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td>Path</td><td><code>/ws</code></td></tr><tr><td>Max frame payload</td><td>256 KiB</td></tr><tr><td>Origin check</td><td>Cloud only; the handshake is refused unless <code>Origin</code>&#39;s host matches <code>Host</code> or <code>SAIDI_PUBLIC_URL</code>. A missing <code>Origin</code> falls through to session auth</td></tr><tr><td>Auth (local)</td><td>None. Everything is the single <code>local</code> org</td></tr><tr><td>Auth (cloud)</td><td>The <code>saidi_session</code> cookie rides the same-origin upgrade, plus a <code>?org=&lt;orgId&gt;</code> query parameter</td></tr></tbody></table><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wss://&lt;your-workspace-host&gt;/ws            # same origin as the app</span></span>
<span class="line"><span>wss://your-host/ws?org=&lt;orgId&gt;            # cloud — cookie carries the session</span></span></code></pre></div><p>In cloud mode the socket authenticates <em>before</em> any frame is served: the session is validated, the <code>?org=</code> value is membership-checked, the org must be <code>active</code>, and the socket is then tagged with <code>{ orgId, userId, runsViewAll, chatsViewAll }</code>. A platform superadmin may open any workspace with owner-level visibility.</p><h2 id="close-codes" tabindex="-1">Close codes <a class="header-anchor" href="#close-codes" aria-label="Permalink to &quot;Close codes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>1013</code></td><td>MongoDB is unavailable — the socket refuses to serve or accept frames</td></tr><tr><td><code>4401</code></td><td>Unauthenticated, or the session was revoked (logout, password reset). The client should stop reconnecting and log out</td></tr><tr><td><code>4403</code></td><td>Membership changed or the org was suspended. The client reconnects and re-authenticates with fresh governance</td></tr></tbody></table><p>Sockets cache session, org, role, and permissions at handshake time, so the only way to revoke access is to close them — which is what <code>closeOrgUserSockets</code> / <code>closeUserSocketsEverywhere</code> / <code>closeOrgSockets</code> do.</p><h2 id="connecting" tabindex="-1">Connecting <a class="header-anchor" href="#connecting" aria-label="Permalink to &quot;Connecting&quot;">​</a></h2><p>On a successful connection the server sends one <code>ready</code> frame, then replays snapshot frames so a refreshed tab rejoins any process-global turns already running without restarting the work:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ready&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;projects&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">7</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;turn_start&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;conversationId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;message_state&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;state&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;running&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;conversationId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stream_snapshot&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;eventId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;text&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…so far&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;conversationId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;queue&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,           </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;n&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;conversationId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>Two independent liveness mechanisms run:</p><ul><li><strong>Protocol heartbeat.</strong> The server pings every socket every 30 s and terminates any client that did not answer the previous ping. Terminating fires the connection&#39;s close handler, which deliberately does <em>not</em> abort an in-flight text turn — those are process-global and survive a dropped tab.</li><li><strong>App-level keepalive.</strong> The client may send <code>{ &quot;type&quot;: &quot;ping&quot; }</code> and time the <code>{ &quot;type&quot;: &quot;pong&quot; }</code> reply, which detects a half-open TCP socket that the protocol ping cannot.</li></ul><p>Each socket is rate-limited to 60 frames per second in a fixed 1 s window; anything past the cap is dropped silently. A socket may also target at most 32 distinct chats over its lifetime.</p><h2 id="frames-the-client-sends" tabindex="-1">Frames the client sends <a class="header-anchor" href="#frames-the-client-sends" aria-label="Permalink to &quot;Frames the client sends&quot;">​</a></h2><table tabindex="0"><thead><tr><th><code>type</code></th><th>Fields</th><th>Effect</th></tr></thead><tbody><tr><td><code>ping</code></td><td>—</td><td>Answered with <code>pong</code>, nothing else</td></tr><tr><td><code>user</code></td><td><code>text</code>, <code>id?</code>, <code>conversationId?</code>, <code>attachments?</code>, <code>mode?</code></td><td>Enqueue a main-chat turn. <code>mode: &#39;voice&#39;</code> marks it as a voice turn</td></tr><tr><td><code>cancel</code></td><td><code>id</code></td><td>Cancel the named turn (queued or active)</td></tr><tr><td><code>reset</code></td><td><code>conversationId?</code></td><td><code>/clear</code> — flush the queue, drop the session, erase that chat&#39;s durable activity</td></tr><tr><td><code>confirm_response</code></td><td><code>id</code>, <code>approved</code></td><td>Answer a <code>confirm</code> card</td></tr><tr><td><code>realtime_transcript</code></td><td><code>turnId</code>, <code>role</code> (<code>user</code> | <code>assistant</code>), <code>text</code></td><td>Record a voice turn&#39;s transcript; an <code>assistant</code> role releases the turn</td></tr><tr><td><code>realtime_tool_call</code></td><td><code>turnId</code>, <code>callId</code>, <code>name</code>, <code>arguments?</code></td><td>Execute one PRIME tool for the voice session; answered with <code>realtime_tool_result</code></td></tr><tr><td><code>realtime_cancel</code></td><td><code>turnId</code></td><td>Abort the voice turn and release its liveness</td></tr></tbody></table><p><code>attachments</code> entries are <code>{ file, alt? }</code> and are only accepted if the file already exists in the chat uploads directory (upload it over HTTP first). At most 12 per message.</p><p>A <code>conversationId</code> you may not access is <strong>dropped</strong>, never silently rewritten to another chat — the server replies with an <code>error</code> frame instead. Only the literal placeholder <code>&#39;main&#39;</code> maps to your own default chat.</p><h2 id="frames-the-server-sends-—-main-chat" tabindex="-1">Frames the server sends — main chat <a class="header-anchor" href="#frames-the-server-sends-—-main-chat" aria-label="Permalink to &quot;Frames the server sends — main chat&quot;">​</a></h2><table tabindex="0"><thead><tr><th><code>type</code></th><th>Payload</th><th>Meaning</th></tr></thead><tbody><tr><td><code>ready</code></td><td><code>projects</code></td><td>Connection established</td></tr><tr><td><code>pong</code></td><td>—</td><td>Keepalive answer</td></tr><tr><td><code>queue</code></td><td><code>n</code>, <code>conversationId</code></td><td>Queue depth changed</td></tr><tr><td><code>message_state</code></td><td><code>id</code>, <code>state</code> (<code>queued</code> | <code>running</code>), <code>position?</code></td><td>One message&#39;s lifecycle</td></tr><tr><td><code>turn_start</code></td><td><code>id</code>, <code>conversationId</code></td><td>A turn began</td></tr><tr><td><code>delta</code></td><td><code>id</code>, <code>eventId</code>, <code>text</code></td><td>A streaming text chunk for the live segment</td></tr><tr><td><code>thinking</code></td><td><code>id</code>, <code>text</code></td><td>Reasoning text for the current turn</td></tr><tr><td><code>status</code></td><td><code>id</code>, <code>eventId?</code>, <code>actor</code>, <code>text</code>, <code>detail?</code></td><td>A tool step or delegation line</td></tr><tr><td><code>working</code></td><td><code>id</code>, <code>elapsedMs</code>, <code>detail?</code></td><td>Heartbeat while a long turn runs</td></tr><tr><td><code>chat_event</code></td><td><code>event</code></td><td>A durable transcript row (<code>id</code>, <code>actor</code>, <code>msg</code>, <code>detail</code>, <code>at</code>, <code>attachments?</code>, <code>stats?</code>)</td></tr><tr><td><code>stream_snapshot</code></td><td><code>id</code>, <code>eventId</code>, <code>text</code></td><td>Full live-segment text, sent on reconnect</td></tr><tr><td><code>vitals</code></td><td><code>id</code>, <code>models</code>, <code>ctx</code>, <code>tokens</code>, <code>cache</code>, <code>ctxTokens</code>, <code>ctxWindow</code>, <code>cost</code></td><td>Settled numbers for the turn</td></tr><tr><td><code>confirm</code></td><td><code>id</code>, <code>turnId</code>, <code>text</code></td><td>An approval card; answer with <code>confirm_response</code></td></tr><tr><td><code>cancel_requested</code></td><td><code>id</code></td><td>Cancellation acknowledged, not yet effective</td></tr><tr><td><code>turn_cancelled</code></td><td><code>id</code>, <code>phase</code> (<code>queued</code> | <code>active</code>)</td><td>The turn was cancelled</td></tr><tr><td><code>turn_end</code></td><td><code>id</code>, <code>cancelled</code>, <code>cleared</code></td><td>The turn settled</td></tr><tr><td><code>cleared</code></td><td><code>conversationId</code></td><td>The chat was reset</td></tr><tr><td><code>error</code></td><td><code>text</code>, <code>id?</code>, <code>conversationId?</code></td><td>A frame was refused</td></tr></tbody></table><p>Two frames are not chat-specific:</p><table tabindex="0"><thead><tr><th><code>type</code></th><th>Payload</th><th>Meaning</th></tr></thead><tbody><tr><td><code>agent_state</code></td><td><code>actor</code>, <code>state</code> (<code>idle</code> | <code>active</code> | <code>thinking</code>)</td><td>Lights or clears a node in the orchestration graph</td></tr><tr><td><code>state_dirty</code></td><td>—</td><td>Re-pull <code>/api/state</code>. Sent when the registry, roster, or any store changes out of band</td></tr><tr><td><code>wallet</code></td><td>—</td><td>The wallet moved. Deliberately amount-free, so each dashboard re-fetches a balance scoped to its own viewer</td></tr></tbody></table><h2 id="frames-the-server-sends-—-runs" tabindex="-1">Frames the server sends — runs <a class="header-anchor" href="#frames-the-server-sends-—-runs" aria-label="Permalink to &quot;Frames the server sends — runs&quot;">​</a></h2><table tabindex="0"><thead><tr><th><code>type</code></th><th>Payload</th><th>Meaning</th></tr></thead><tbody><tr><td><code>run_update</code></td><td><code>run</code> (a <code>RunSummary</code>)</td><td>Durable state changed — status, participants, branch — <strong>or</strong> a live <code>lastActivity</code> progress line</td></tr><tr><td><code>run_event</code></td><td><code>runId</code>, <code>event</code> (a <code>RunEvent</code>)</td><td>A new committed thread entry</td></tr><tr><td><code>run_delta</code></td><td><code>runId</code>, <code>streamId</code>, <code>actor</code>, <code>text</code>, <code>seq</code></td><td>A streaming token chunk for the live segment</td></tr><tr><td><code>run_segment_end</code></td><td><code>runId</code>, <code>streamId</code></td><td>The live segment closed; its final text arrives as a <code>run_event</code></td></tr><tr><td><code>run_truncated</code></td><td><code>runId</code>, <code>fromEventId</code></td><td>A rewind — drop this event and everything after it</td></tr><tr><td><code>run_removed</code></td><td><code>runId</code></td><td>The run was permanently deleted</td></tr></tbody></table><p>Every thread entry — messages, worker text blocks, tool rows, results — goes through <code>runs.appendEvent</code>, which persists it and broadcasts it exactly once via the store&#39;s single observer. There is no separate &quot;ephemeral thread event&quot; path, so live and saved can never diverge. Only <code>run_delta</code> and the live activity line are ephemeral by design.</p><p><code>run_delta</code> carries a per-stream <code>seq</code> counter, incremented per frame and reset when the segment ends. A frame delivered twice (a ghost socket in a long-lived tab, a reconnect overlap) repeats its <code>seq</code>, so clients drop it instead of garbling the live text.</p><h3 id="how-a-client-subscribes-to-a-run" tabindex="-1">How a client subscribes to a run <a class="header-anchor" href="#how-a-client-subscribes-to-a-run" aria-label="Permalink to &quot;How a client subscribes to a run&quot;">​</a></h3><p>There is <strong>no subscribe frame</strong>. Subscription is implicit and enforced server-side:</p><ol><li>Open <code>/ws</code> (with <code>?org=</code> in cloud mode).</li><li>Fetch the run over HTTP — <code>GET /api/runs/:id</code> returns the full thread.</li><li>Apply every subsequent frame whose <code>runId</code> matches; ignore the rest.</li></ol><p>The server decides what reaches you. Content-bearing run frames go only to sockets that may view the run: the creator&#39;s, anyone in <code>run.sharedWith</code>, and any member holding <code>runs.viewAll</code> in that org. Everything else is filtered out before the send, so a member never receives another member&#39;s un-shared run stream — including live token deltas, which mirror the committed-event path exactly.</p><p>Main-chat frames use the same mechanism with <code>chats.viewAll</code>.</p><h2 id="runevent-—-the-durable-thread-entry" tabindex="-1"><code>RunEvent</code> — the durable thread entry <a class="header-anchor" href="#runevent-—-the-durable-thread-entry" aria-label="Permalink to &quot;\`RunEvent\` — the durable thread entry&quot;">​</a></h2><p>The payload inside a <code>run_event</code> frame, defined in the run object reference.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Meaning</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>string</code></td><td>Event id</td></tr><tr><td><code>at</code></td><td><code>number</code></td><td>Epoch ms</td></tr><tr><td><code>actor</code></td><td><code>string</code></td><td>Who produced it</td></tr><tr><td><code>kind</code></td><td>see below</td><td>What kind of entry it is</td></tr><tr><td><code>to?</code></td><td><code>string</code></td><td>Directed-at target</td></tr><tr><td><code>text</code></td><td><code>string</code></td><td>The message</td></tr><tr><td><code>detail?</code></td><td><code>string</code></td><td>Full tool call/output for the expandable row</td></tr><tr><td><code>attachments?</code></td><td><code>Attachment[]</code></td><td><code>{ kind: &#39;image&#39; | &#39;file&#39;, file, alt? }</code></td></tr><tr><td><code>byUser?</code></td><td><code>string</code></td><td>For an <code>OPERATOR</code> event, the member who sent or decided it</td></tr><tr><td><code>stats?</code></td><td><code>RunEventStats</code></td><td>This message&#39;s own token/cost vitals</td></tr></tbody></table><p><strong>Actors</strong> are <code>OPERATOR</code> (the human), <code>PRIME</code>, a project or agent name, or <code>SYSTEM</code>.</p><p><strong>Kinds:</strong></p><table tabindex="0"><thead><tr><th><code>kind</code></th><th>Meaning</th></tr></thead><tbody><tr><td><code>message</code></td><td>A normal message — operator to PRIME, PRIME back, or a running agent&#39;s summary</td></tr><tr><td><code>delegation</code></td><td>PRIME handed work to an agent</td></tr><tr><td><code>question</code></td><td>An agent asked something back</td></tr><tr><td><code>answer</code></td><td>The resolved answer to a question</td></tr><tr><td><code>tool</code></td><td>A tool step (Read/Edit/Bash…) with detail</td></tr><tr><td><code>result</code></td><td>A tool&#39;s output</td></tr><tr><td><code>system</code></td><td>A system note — branch cut, run paused, cap reached</td></tr></tbody></table><p>Field-by-field coverage of the surrounding run record is in <a href="/docs/reference/run-object">The Run Object</a>.</p><h2 id="automation-frames" tabindex="-1">Automation frames <a class="header-anchor" href="#automation-frames" aria-label="Permalink to &quot;Automation frames&quot;">​</a></h2><table tabindex="0"><thead><tr><th><code>type</code></th><th>Payload</th><th>Meaning</th></tr></thead><tbody><tr><td><code>cron_update</code> / <code>cron_removed</code></td><td><code>cron</code> / <code>cronId</code></td><td>The cron registry changed</td></tr><tr><td><code>reflex_update</code> / <code>reflex_removed</code></td><td><code>reflex</code> / <code>reflexId</code></td><td>The reflex registry changed</td></tr><tr><td><code>reflex_event</code></td><td><code>event</code> (a <code>RecentEvent</code>)</td><td>An event was seen on the bus, with how many reflexes matched it</td></tr><tr><td><code>reflex_fire</code></td><td><code>fire</code> (a <code>ReflexFire</code>)</td><td>A fire-log entry: <code>fired</code> | <code>skipped</code> | <code>pending</code> | <code>denied</code> | <code>error</code></td></tr><tr><td><code>reflex_fired</code></td><td><code>reflexId</code>, <code>reflexName</code>, <code>event</code>, <code>runId?</code></td><td>A reflex fired</td></tr><tr><td><code>reflex_pending</code></td><td><code>pending</code></td><td>A fire is parked awaiting approval</td></tr><tr><td><code>reflex_resolved</code></td><td><code>pendingId</code>, <code>resolution</code></td><td>That approval was decided</td></tr></tbody></table><h2 id="the-bus-event-envelope" tabindex="-1">The bus event envelope <a class="header-anchor" href="#the-bus-event-envelope" aria-label="Permalink to &quot;The bus event envelope&quot;">​</a></h2><p>Every producer — the run engine, webhook ingress, pollers, agents — reduces its raw payload to one normalized <code>Event</code> and hands it to <code>emitEvent()</code>. This is the shape reflex rules match against.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// unique — used for dedup / idempotency</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// epoch ms</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  source</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// event family</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// event type within the source</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  actor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// who/what triggered it</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// normalized, matchable fields</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  raw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the untouched original, for audit / re-parse</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  runId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// set when it originated inside a run</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  depth</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// chain depth; the bus loop-guard caps it</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  orgId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// owning org — matching is same-org scoped</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="real-source-values" tabindex="-1">Real <code>source</code> values <a class="header-anchor" href="#real-source-values" aria-label="Permalink to &quot;Real \`source\` values&quot;">​</a></h3><table tabindex="0"><thead><tr><th><code>source</code></th><th>Producer</th></tr></thead><tbody><tr><td><code>run</code></td><td>The run-lifecycle producer (<code>reflexes/engine.ts</code>)</td></tr><tr><td><code>agent</code></td><td>An agent or PRIME calling the <code>raise_event</code> tool. Always emitted at <code>depth: 1</code></td></tr><tr><td><code>generic</code></td><td><code>POST /api/webhooks/generic</code></td></tr><tr><td><code>github</code></td><td><code>POST /api/webhooks/github</code></td></tr><tr><td><code>stripe</code></td><td><code>POST /api/webhooks/stripe</code></td></tr><tr><td><code>whatsapp</code>, <code>instagram</code>, <code>messenger</code></td><td>Meta ingress, resolved from the payload or the URL you posted to</td></tr><tr><td><em>any name</em></td><td>The env-configured webhook path — <code>POST /api/webhooks/&lt;name&gt;</code> with <code>SAIDI_WEBHOOK_&lt;NAME&gt;_*</code> set</td></tr><tr><td><em>operator-chosen</em></td><td>A reflex whose action is <code>emitEvent</code> sets its own <code>source</code>, at <code>depth + 1</code></td></tr></tbody></table><h3 id="real-run-types" tabindex="-1">Real <code>run</code> types <a class="header-anchor" href="#real-run-types" aria-label="Permalink to &quot;Real \`run\` types&quot;">​</a></h3><table tabindex="0"><thead><tr><th><code>type</code></th><th>Emitted when</th></tr></thead><tbody><tr><td><code>run.finished</code></td><td>The run reached <code>done</code></td></tr><tr><td><code>run.failed</code></td><td>The run reached <code>error</code></td></tr><tr><td><code>run.needs_approval</code></td><td>The run is <code>awaiting_input</code> <strong>and</strong> holds a pending approval gate</td></tr><tr><td><code>run.cost_exceeded</code></td><td>The run&#39;s cumulative cost crossed <code>runCostAlertUsd</code>. Fires once per run</td></tr></tbody></table><p>Payload for the first three: <code>{ runId, title, status, message }</code>. For <code>run.cost_exceeded</code>: <code>{ runId, title, costUsd, threshold }</code>. All four carry <code>actor</code> = the run title and <code>runId</code> = the run.</p><p>Paused, cancelled, and plain <code>awaiting_input</code> transitions emit nothing.</p><p>Naming for webhook types follows the source: GitHub becomes <code>&lt;x-github-event&gt;.&lt;action&gt;</code> (e.g. <code>pull_request.opened</code>), Stripe passes its own <code>type</code> through (e.g. <code>payment_intent.succeeded</code>), and Meta flattens to <code>whatsapp.text</code>, <code>whatsapp.status.delivered</code>, <code>messenger.postback</code>, and so on.</p><h3 id="what-the-bus-does-with-an-event" tabindex="-1">What the bus does with an event <a class="header-anchor" href="#what-the-bus-does-with-an-event" aria-label="Permalink to &quot;What the bus does with an event&quot;">​</a></h3><p><code>emitEvent()</code> is the single seam, and the one place safety lives:</p><table tabindex="0"><thead><tr><th>Guard</th><th>Behaviour</th></tr></thead><tbody><tr><td>Dedup</td><td>The <code>orgId:eventId</code> pair is remembered (2000 ids). A repeat is dropped — retried webhooks and overlapping pollers are free</td></tr><tr><td>Loop guard</td><td><code>depth &gt; 3</code> is dropped and logged</td></tr><tr><td>Rate limit</td><td>120 events per source per 60 s window, keyed per org, so one tenant&#39;s flood cannot throttle another&#39;s</td></tr><tr><td>Per-reflex</td><td>Max-fires cap, cooldown, dedup key, and the approval gate, in that order</td></tr></tbody></table><p>Dedup and rate-limit keys are always org-prefixed; a single workspace collapses to the single <code>local</code> key. Every event that reaches the matcher is logged with how many reflexes matched it, which is what feeds the <code>reflex_event</code> frame.</p><p>A matched reflex reserves its fire slot <strong>synchronously</strong> — <code>fireCount++</code>, <code>lastFiredAt</code>, auto-disable at the cap — before its async action dispatches, so a burst arriving in one tick cannot read stale counters and blow past <code>maxFires</code>. If the action then fails, the reservation is rolled back so a failure does not consume the cap.</p><h2 id="raising-an-event-by-hand" tabindex="-1">Raising an event by hand <a class="header-anchor" href="#raising-an-event-by-hand" aria-label="Permalink to &quot;Raising an event by hand&quot;">​</a></h2><p>Any agent (PRIME included) can put an event on the bus with the <code>raise_event</code> tool — see <a href="/docs/reference/orchestrator-tools">Orchestrator Tools</a>. It is fixed to <code>source: &#39;agent&#39;</code> and <code>depth: 1</code>, so it is loop-guarded like anything else. The HTTP equivalent for external systems is the webhook ingress.</p><p>See also: <a href="/docs/concepts/reflexes-and-events">Reflexes &amp; Events</a> · <a href="/docs/concepts/runs">Runs</a></p>`,62)])])}const u=t(o,[["render",i]]);export{k as __pageData,u as default};
